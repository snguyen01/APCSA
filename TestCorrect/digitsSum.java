
/**
 * Write a description of class digitsSum here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class digitsSum
{
    int length = String.valueOf(number).length();
    int newValue;
    int digit;
    int sum = 0;
    newValue = value;
    
    while(i = 0; i<length; i++){
        digit = newValue%10;
        sum = sum + digit;
        newValue = newValue/10;
}
return sum;
}